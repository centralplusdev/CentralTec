function abrirTutorial(categoria) {
    if (categoria === "Servidores") {
        window.location.href = "servidores.html";
    }

    if (categoria === "Discord") {
        alert("Os tutoriais de Discord serão adicionados em breve!");
    }

    if (categoria === "Web") {
        alert("Os tutoriais de Web serão adicionados em breve!");
    }
}
